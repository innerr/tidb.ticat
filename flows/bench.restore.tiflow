help = restore workload data to cluster

flow = tidb.clean : [[bench.workload]].gen-tag : tidb.backup.gen-tag : [[tidb.backupper]].restore : tidb.start

[arg]
backupper = tidb.ssh

[arg2env]
tidb.backupper = backupper

[env]
bench.workload = read
