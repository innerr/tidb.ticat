help = restore workload data to cluster, then run workload

flow = tidb.clean : [[bench.workload]].gen-backup-tag : tidb.backup.restore : tidb.start : [[bench.workload]].run

[env]
bench.workload = read
