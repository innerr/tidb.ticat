help = setup workload threads (same as '<workload>.config.threads <threads>')

abbr = bench.threads.conf

flow = [[bench.workload]].config.threads [[bench.threads.value]]

[arg]
threads|thread|t|T = ''

[env]
bench.workload = read
bench.threads.value = read

[arg2env]
bench.threads.value = threads
