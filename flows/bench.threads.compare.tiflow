help = compare bench result of different threads in a list seperated by ','
abbr = bench.threads.cmp

flow = **[[bench.workload]].config.threads [[*bench.threads*]] : [[sub-flow]] : **

[arg]
sub-flow|sub = bench.run
threads|t|T = 1,2

[arg2env]
bench.threads = threads

[env]
bench.workload = read
bench.threads = read
